#include <stdlib.h>
#include <stdio.h>

int main() {
      srand(42); 

    // Generování náhodného čísla
    int randomNumber = rand();

    printf("Náhodné číslo: %d\n", randomNumber);

    return 0;
}